require "spec_helper"

RSpec.describe Gnar::Style do
  it "has a version number" do
    expect(Gnar::Style::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
